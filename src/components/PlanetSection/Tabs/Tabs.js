import { StyledTabs, Button } from './Tabs.styled';

const Tabs = () => {
  return (
    <StyledTabs>
      <Button>Overview</Button>
      <Button>Overview</Button>
      <Button>Overview</Button>
    </StyledTabs>
  );
};

export default Tabs;
